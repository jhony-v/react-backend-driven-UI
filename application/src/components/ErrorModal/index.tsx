import React from "react";
import styled from "styled-components";

const S = styled.div({
  fontSize: "3em",
  margin: "auto",
  textAlign: "center",
});

const ErrorModal = () => {
  return <S>Error to get data</S>;
};

export default ErrorModal;
